{
  "name": "@polymesh/srb-test",
  "version": "1.0.0",
  "description": "Dummy TypeScript package for testing semantic-release",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepare": "npm run build",
    "release:dry": "semantic-release --dry-run",
    "release:dry:alpha": "semantic-release --dry-run --branches alpha",
    "release:dry:next": "semantic-release --dry-run --branches next",
    "release": "semantic-release --no-ci --debug",
    "release:alpha": "semantic-release --no-ci --debug --branches alpha",
    "release:next": "semantic-release --no-ci --debug --branches next"
  },
  "keywords": [
    "semantic-release",
    "test"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.19",
    "jest": "^29.7.0",
    "semantic-release": "^23.0.2",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^11.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^9.2.6",
    "@semantic-release/npm": "^11.0.2",
    "@semantic-release/release-notes-generator": "^12.1.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mrccode/srb-test.git"
  }
}
